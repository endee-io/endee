# Endee Vector Database
ENDEE_BASE_URL=http://localhost:8080/api/v1
ENDEE_AUTH_TOKEN=        # Leave blank for open mode (default)

# LLM Configuration
# Option A: OpenAI (paid, best quality)
OPENAI_API_KEY=your_openai_key_here

# Option B: Leave OPENAI_API_KEY blank to auto-fallback to
# local google/flan-t5-base model (free, no API key needed)
